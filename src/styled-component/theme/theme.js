import mediaQuery from './mediaQuery'
const fontSize = {
  sm: '12px',
  md: '16px',
  lg: '20px',
  xl: '24px',
}

const Color = {
  primary: '#ea3267',
}

const theme = {
  mediaQuery,
  fontSize,
  Color,
}

export default theme
